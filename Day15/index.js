let w = window.innerWidth;
let h = window.innerHeight;

document.getElementById("demo").innerHTML = "width = " + w + " and height = " + h;

function getScreenStatus() {
    alert("width = " + w + " and height = " + h);
}